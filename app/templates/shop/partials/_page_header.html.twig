{% import 'shop/macros/heading.html.twig' as ui %}

{% if pageTitle is defined and pageTitle is not null %}
    <header class="bg-background-base border-t border-b border-gray-200 mb-[2.5rem] lg:border-t-0">
        <div class="lg:container px-4 py-5 lg:mx-auto lg:px-auto flex gap-2 flex-col items-center lg:flex-row lg:justify-between">
            {{ ui.heading('h1', pageTitle, 'text-primary-content') }}
            <span>
                BREADCRUMB
            </span>
        </div>
    </header>
{% endif %}
